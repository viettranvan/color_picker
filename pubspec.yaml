name: color_picker
description: Pick colors from your application anywhere easily and conveniently
version: 0.0.1
repository: https://github.com/viettranvan/color_picker

environment:
  sdk: '>=2.19.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  image: ^4.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  